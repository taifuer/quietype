# 与 AI Agent 一起从零打磨一款 WordPress 阅读主题

这是一份 Quietype 的项目复盘，而不是“输入一句提示词，AI 自动生成主题”的案例。主题的方向、取舍和验收始终由站点所有者决定；AI Agent 参与了资料调研、静态原型、WordPress 实现、故障排查、测试、部署与文档整理。双方围绕一个真实运行多年的中文技术博客持续迭代，才逐渐得到现在的结果。

仓库从 2026 年 7 月 19 日的首个可运行版本开始记录历史。到 2026 年 8 月初，Quietype 经历了 63 次提交，并在真实文章、评论、书籍、照片、缓存和移动设备上反复验证。它仍然只是一个持续维护中的个人项目，但这段过程留下了不少可以复用的经验。

## 一、我们真正想解决什么

起点不是“再做一个 WordPress 主题”，而是改善已有付费主题未能充分解决的阅读问题：中文长文的行宽和节奏、Markdown 技术内容、桌面目录、移动导航、代码与公式、搜索和归档，以及不稳定网络环境下的资源加载。

项目逐渐形成了几条稳定约束：

- 简约不等于简陋，留白、字号、边界和反馈都要有明确理由；
- 明亮、素雅、中文优先，不追逐视觉潮流，也不为了“功能齐全”增加干扰；
- 历史 Markdown 和 WordPress 数据不能被主题锁定；
- 桌面端与移动端分别设计，不能只把桌面页面缩小；
- 真实内容和真实设备的反馈优先于静态稿；
- 网络请求、外部服务和安全增强必须可选、可解释、可回退。

## 二、从原型到完整主题

| 阶段 | 主要工作 | 最重要的认识 |
| --- | --- | --- |
| 研究 | 比较 WordPress、Hexo、Hugo、Halo、Typecho 和 Typora 主题 | 学习信息层级和排印方法，不拼接某个现成主题 |
| 原型 | 先做首页、文章、归档、友链等静态效果 | 静态稿适合快速决定密度、轴线、字号和背景 |
| 主题化 | 接入 WordPress 模板层级、菜单、评论、搜索和设置 | 前台效果只是开始，数据、后台和兼容性决定能否长期使用 |
| 真实验证 | 在既有文章、公式、代码、图片和多级评论上迭代 | 演示内容发现不了长标识符、旧插件脚本和缓存等问题 |
| 扩展 | 增加书架、图库、友链状态、邮件、SEO 和安全设置 | 新能力必须服从主题气质，并尽量沿用 WordPress 数据模型 |
| 工程化 | Docker、Playwright、axe、HTML 校验、视觉快照、Lighthouse 和 CI | Agent 可以高频修改，但必须用自动化阻止高频回归 |
| 开源整理 | 删除站点私有默认值，补齐配置、许可、贡献和发布流程 | “自己的站能用”与“别人可以安全安装”是两个标准 |

### 1. 先确定阅读系统

Quietype 最终采用 960px 主布局、约 760–800px 的长文栏，并在宽屏把目录放到正文右侧。文章标题和正文左轴保持一致，目录可以超出正文但不突破整体页面秩序。纸白、米杏、浅绿三套背景共享同一套语义变量；默认纸白不是刺眼的屏幕纯白，而是很轻的暖白。

字体使用本地系统栈：中文正文优先系统黑体，展示标题适度使用宋体气质，代码使用可用的等宽字体。没有为了截图效果加载网络字体，因为字体闪烁、区域网络和隐私成本不值得。

信息层级也经历了多轮删减：首页去掉大段宣言和多余引导，文章元信息按分类、标签、发表/更新日期、字数、阅读时间和浏览量分组；分类使用编辑式竖线，标签使用 `#`，两者不再共用一种“胶囊”。Footer 只承担版权、联系和备案等必要信息，没有加入 `Powered by` 或自动出现的隐私链接。

### 2. 让 Markdown 内容像文章，而不是插件输出

主题始终通过标准 `the_content` 渲染，不修改 WP Editor.md 保存的 Markdown 原文，也不写入私有短代码。主题负责的是最终 HTML 的排版和兼容：标题、段落、列表、表格、引用、脚注、图片、图注、行内代码、代码块、KaTeX、Mermaid 和 MindMap 等。

最费时间的细节之一是中英文与技术标识符换行。简单使用 `word-break: break-all` 会把普通英文单词任意切碎；完全不处理又会让长 URL、`virConnectDomainEventRegisterAny` 一类标识符整块跳到下一行或撑破移动端。最终做法是只在文本节点的路径、连字符、驼峰等语义位置注入软断点，并对链接、行内代码和普通文本分别处理。这个方案既利用当前行剩余空间，也尽量不破坏复制结果和可读性。

代码样式由主题掌控，Prism 只在存在时负责解析、语言标记和行号。这样不会因为编辑器插件切换高亮主题而让前台突然变样。代码块还增加了语言标签、中文复制按钮、键盘可聚焦的横向滚动和浅色 token 配色。

### 3. 把移动端当成独立界面

移动端改动远多于媒体查询：搜索在菜单左侧，菜单贴最右边；抽屉只占有限宽度，一级菜单默认可见、子菜单按需展开；打开抽屉时锁定根滚动容器，避免页面滚动后 Header 被遮住。触摸按钮在点击后主动释放仅由指针产生的焦点，解决移动浏览器“再次点击仍高亮”的问题。

文章目录在移动端使用原生 `details` 折叠，不显示无价值的章节数量；正文允许缩放但不发生整页横向漂移，代码块保留自己的横向滚动。Footer 联系图标、版权与备案也针对窄屏重新排列，而不是强行压缩桌面布局。

## 三、值得保留的功能与实现

### 阅读体验

- H2/H3 自动目录、当前章节高亮、吸顶 Header 偏移和标题永久链接；
- 阅读进度、返回顶部/底部、三种亮色背景与本地偏好；
- 克制的文章元信息、版权声明、上下篇和多级评论；
- 图片边框、图注、响应式尺寸、打印样式和 PhotoSwipe 灯箱；
- 文章、分类、标签、搜索、归档、友链、关于、作者与 404 模板。

### WordPress 集成

- 集中的 Quietype 设置页，同时尊重菜单、隐私页和额外 CSS 等原生入口；
- 轻量 SEO、Open Graph 和 Schema.org，并在检测到专业 SEO 插件时整体让位；
- AJAX 浏览量计数，复用历史 `views` 元数据，同时绕开整页缓存；
- 可选 SMTP、测试邮件和登录/评论通知，错误信息短期保存且不记录密码；
- 自定义登录入口、签名 Cookie、评论挑战、蜜罐、节流和单作者隐私选项；
- 文章、页面和书籍的版本控制与显式清理，不在后台静默删除历史数据。

### 书架、图库与友链

书架和图库都采用 WordPress 自定义内容类型，但不提供无价值的独立详情页。书籍按阅读年份组织，支持阅读状态、分类、标签、短评、个人整星、外部资料和可选推荐标记；豆瓣读取只是管理员主动触发的预览，必须再次确认才能填入，抓取失败时仍可完全手工维护。

图库按年份折叠，列表请求 WebP 缩略图，灯箱才请求展示图，原图需要访客主动打开。外链图片缺失时会回退，PhotoSwipe 本地分发，不依赖第三方 CDN 脚本。相机信息会去掉 GPS，手机型号可归一为品牌，避免把不必要的设备细节放到前台。

友链沿用 WordPress 原生 Links 与分类。可达性检测是分批、SSRF 安全且保守的：连续失败三次只提示管理员复核，不自动给朋友的站点判“失联”；最终公开状态仍由人确认。

## 四、踩过的坑

### 1. CSS 补丁会迅速失控

早期为了快速响应视觉反馈，同一选择器被多次追加覆盖，变量、文章行高、分类样式和移动间距出现多份“最终值”。页面看似正确，但任何新调整都可能命中旧规则。0.8.0 专门进行了一次无损收敛：按组件重排最终样式、删除死代码、合并重复媒体查询，并用视觉快照保证外观不变。

可复用的教训是：探索期允许补丁，设计稳定后必须安排一次独立重构；不要一边重构一边改视觉，否则无法判断回归来自哪里。

### 2. 插件兼容不能只靠 CSS 覆盖

WP Editor.md 可能按设置加载 Prism、KaTeX、Mermaid、MindMap、Plyr 和 jQuery。曾出现插件初始化脚本存在、依赖却被条件移除，最终产生 `jQuery is not defined`；也出现插件行号内边距和主题代码样式互相覆盖。后来改为内容感知的资源保留/移除，并明确主题与插件的职责边界。

判断某个资源“看起来没用”之前，必须同时检查它的初始化脚本、依赖关系、后台设置和历史文章，而不是只看当前页面有没有对应 DOM。

### 3. 懒加载和灯箱是两套状态机

图库最棘手的问题来自 Edge 的原生懒加载占位、折叠年份、CDN 缓存和 PhotoSwipe 相邻预加载叠加：隐私窗口正常，常规窗口却有部分图片空白；`aria-hidden` 的变化只是结果，不是原因。最终将网格缩略图、灯箱展示图和原图请求分开，JavaScript 延迟图片不再同时标记原生 lazy，并为偶发 CDN 失败提供一次带缓存规避的重试。

经验是：不要让浏览器懒加载、应用懒加载和灯箱预加载同时控制同一个 `img`；应明确每一层何时创建请求、失败后由谁接管。

### 4. 缓存会让“保存成功”看起来像失败

浏览量不能依赖被 FastCGI 缓存的文章 HTML，因此改为独立 AJAX 更新并进行浏览器/服务端去重。书籍和照片保存后，归档页缓存也要显式失效；仅降低缓存时间会让后台行为变得不可预测。资源 URL 使用文件修改时间做版本号，避免一个月的静态缓存继续提供旧 CSS。

### 5. 小交互必须用真实手机验证

右下角工具的分隔线、按钮尺寸、Footer 遮挡、触摸高亮、菜单滚动锁定、移动灯箱箭头和浏览量换行，都曾在桌面模拟中看似合理、在手机上明显不协调。截图回归能阻止已知布局倒退，但不能代替触摸手势、浏览器返回和缓存状态的人工检查。

### 6. “安全增强”不能制造虚假的安全感

隐藏登录入口和验证码只能降低机器人噪音，不能替代强密码、MFA、服务器限速、及时更新与备份。自定义入口还必须保留找回密码、重置、退出和 WordPress 签名流程，否则很容易把管理员自己锁在门外。评论挑战最初在页面渲染时写 transient，也不利于整页缓存；后来改成缓存友好的签名挑战，只在验证阶段落状态。

### 7. WordPress 默认值有自己的语义

某些默认勾选项保存后看似“恢复未勾选”，原因是 WordPress 可能优化掉与注册默认值相同的 option。读取设置时必须区分“从未保存”“显式关闭”和“值等于默认”，不能简单把 option 不存在当成 false。

## 五、人和 Agent 怎样协作更有效

这段开发最有效的循环并不是“一次性写完”，而是：提出可观察的问题 → Agent 调研和实现 → 本地截图/自动测试 → 真实站点验证 → 根据具体差异继续收敛。

几个实践尤其重要：

1. **用目标语言描述感受，再给可定位的例子。**“简约但不简陋”决定方向；具体文章、选择器、设备和截图帮助定位。
2. **先评估有争议的功能。**暗色模式、文章封面、段首缩进、AI 摘要、相关阅读、Footer 品牌链接等都经过讨论，有些明确不做。少做同样是设计成果。
3. **让 Agent 检查实际输出。**只改 CSS 不截图、只写 PHP 不访问页面，最容易产生“代码正确、体验错误”。
4. **把线上内容当作测试集，但不把私人内容写进仓库。**公开截图最终改为固定演示数据和许可清晰的素材。
5. **每次部署都可回滚。**先备份主题和数据库，再同步有限文件，校验哈希、权限、缓存、控制台和关键统计。
6. **用自动化约束 Agent 的修改速度。**PHP 语法与 WPCS、JavaScript 语法、HTML、axe、Playwright、视觉快照和 Lighthouse 形成多层质量门。
7. **保留人的最终判断。**自动抓取书籍要手动确认，友链失效要手动确认，五星推荐可以自动建议但允许取消；这些都是对“自动化边界”的设计。

## 六、参考与鸣谢

Quietype 没有复刻某一个主题。研究阶段重点观察了以下项目和站点在信息密度、中文排版、导航、目录、归档和留白方面的处理：

- [Retypeset](https://retypeset.radishzz.cc/)、[Hugo PaperMod](https://adityatelange.github.io/hugo-PaperMod/)、[Hugo Stack](https://demo.stack.cai.im/)、[Adams](https://github.com/Tokinx/Adams)；
- [搬砖日记](https://www.banzhuanriji.com/)、[笔记](https://biji.io/)、[三迹](https://www.sanji.one/)、[Moeyua](https://blog.moeyua.com/)、[Joway](https://blog.joway.io/)；
- [writings.sh 的存档页面](https://web.archive.org/web/20250807154239/https://writings.sh/)与 [Halo 主题商店](https://www.halo.run/store/apps?sort=status.downloadCount%2Cdesc&tag=blog&type=THEME)；
- Typora 主题 [Lightmind](https://theme.typora.io/theme/Lightmind/)、[Crisp](https://theme.typora.io/theme/Crisp/)、[Paperglow](https://theme.typora.io/theme/Paperglow/)、[Notion Style](https://theme.typora.io/theme/Notion-Style/)、[Drake](https://theme.typora.io/theme/Drake/)、[Lark](https://theme.typora.io/theme/Lark/) 和 [Inside](https://theme.typora.io/theme/Inside/)，以及 [typora-notion-theme](https://github.com/adrian-fuertes/typora-notion-theme) 与 [typora-ladder-theme](https://github.com/guangzhengli/typora-ladder-theme)；
- Swallow、[NiceTheme](https://www.nicetheme.cn/store) 等商业 WordPress 主题帮助我们比较后台配置、登录保护和站点运营能力的边界。

工程实现建立在 [WordPress](https://wordpress.org/) 生态之上，并兼容 WP Editor.md、Prism 与 KaTeX 的既有内容。图片预览使用 [PhotoSwipe](https://photoswipe.com/)；质量体系使用 [Playwright](https://playwright.dev/)、[axe-core](https://github.com/dequelabs/axe-core)、[Lighthouse CI](https://github.com/GoogleChrome/lighthouse-ci)、[html-validate](https://html-validate.org/) 和 [WordPress Coding Standards](https://github.com/WordPress/WordPress-Coding-Standards)。感谢这些项目及其维护者。

这些参考提供了判断问题的坐标，而 Quietype 的布局、组件、WordPress 集成和测试均围绕自身目标重新实现。仓库内直接分发的第三方代码只有 PhotoSwipe，并保留了对应许可文件。

## 七、给后续维护者的检查清单

- 新样式是否同时检查 390px 与 1440px，并人工查看视觉快照？
- 新内容类型或设置是否清楚说明数据保存位置、停用主题后的行为和迁移方式？
- 新网络请求是否默认关闭、可配置，并在隐私说明中可被识别？
- 外部输入是否经过权限、Nonce、清理、转义以及 SSRF 风险检查？
- 是否尊重 WordPress Core、SEO/缓存插件和既有 Markdown 插件的职责？
- 缓存页面上的计数、验证码和即时状态是否被错误冻结？
- 新依赖是否真的必要、可本地分发、许可兼容，并能按页面条件加载？
- 部署前是否有数据库与主题备份，部署后是否验证哈希、权限、缓存和浏览器控制台？

## 结语

Quietype 的完成度来自许多看似很小的纠正：一段多余留白、一个提前换行的英文标识符、一条重复边框、一个手机上不消失的高亮、一次被缓存吞掉的浏览量。AI Agent 擅长快速调查、实现和重复验证，人则更适合持续定义“什么值得保留”。把两者放进可回滚、可测试、以真实内容为准的工作流，才是这次从零打磨主题最值得复用的部分。
